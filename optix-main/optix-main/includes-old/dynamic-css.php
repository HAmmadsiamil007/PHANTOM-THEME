<?php
/**
 * Backward compatibility shim for optix-core Dynamic CSS Generator.
 *
 * Previously located in the theme, now migrated to the plugin.
 *
 * @package optix
 */

_deprecated_file( __FILE__, '1.0.0', 'OptixCore\Dynamic_CSS_Generator' );

// The plugin's Dynamic_CSS_Generator is loaded automatically via optix-core.php
// This shim exists only to prevent fatal errors if code still references the old path.

